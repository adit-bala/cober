# Instructions (only first time)

- run `npm install`

# committing

- ALWAYS PULL before committing

- open new branch (name for new feature) and switch (never commit on main) 
- once ready notify adit, and then we can merge
